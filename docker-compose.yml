services:
  fleetops:
    image: kratikjain12/fleetops:latest
    container_name: fleetops
    ports:
      - "8000:8000"
    restart: unless-stopped

    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"